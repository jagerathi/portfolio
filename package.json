{
  "name": "kevinknoll-portfolio",
  "version": "1.0.0",
  "description": "Kevin Knoll portfolio + blog (static, terminal theme)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && npx --yes serve ."
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
